geometry:
  size_m: [0.8, 0.5, 0.3]
  color_rgb: [0.05, 0.32, 0.55]
  # Detailed Apache-2.0 visual is loaded only for GUI/camera runs. Physics
  # continues to use this primitive collision size in every mode.
  visual_asset: assets/bluerov2/bluerov2_visual.usda
mass_kg: 20.0
# Explicit contract value; the Fabric-compatible cuboid currently derives the
# PhysX inertia from its collision shape using the same uniform-box model.
inertia_source: uniform_box_approximation
inertia_kg_m2: [0.5666667, 1.2166667, 1.4833333]
center_of_mass_m: [0.0, 0.0, 0.0]
center_of_buoyancy_m: [0.0, 0.0, 0.04]
max_linear_velocity_mps: 4.0
max_angular_velocity_radps: 3.1415926536
solver_position_iterations: 4
solver_velocity_iterations: 1
max_force_n: [80.0, 80.0, 100.0]
max_torque_nm: [18.0, 18.0, 25.0]
thrusters:
  count: 8
  max_forward_thrust_n: 40.0
  max_reverse_thrust_n: 30.0
  dead_zone: 0.05
  curve_exponent: 2.0
  response_time_s: 0.08
